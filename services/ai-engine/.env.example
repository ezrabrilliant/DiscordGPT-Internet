# LM Studio settings (OpenAI-compatible API)
# Start LM Studio -> Load model -> Start Server (port 1234)
LLM_URL=http://localhost:1234/v1
LLM_MODEL=gemma-3n-e4b

# ChromaDB settings
CHROMADB_PATH=./data/chromadb

# Server settings
PORT=8000

# API Key (optional - leave empty to disable auth)
API_KEY=
